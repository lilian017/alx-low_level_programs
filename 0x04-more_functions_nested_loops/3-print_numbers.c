#include <stdio.h>
#include "main.h"

/**
 * print_numbers - prints 0 to 9.
 * Return: The numbers
 */
void print_numbers(void)
{
	int x;

	for (x = 0; x <= 0; x++)
	{
	putchar(x + '0');
	}
	putchar('\n');
}
